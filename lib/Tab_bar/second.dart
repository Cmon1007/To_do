import 'package:flutter/material.dart';
class secondTab extends StatelessWidget {
  const secondTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("In-progress to dos"),
    );
  }
}